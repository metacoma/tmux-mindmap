session_name: relationships
windows:
- window_name: ops
  layout: main-horizontal
  options:
    pane-border-status: top
    pane-border-format: '#{pane_index}: #{pane_title}'
  panes:
  - shell_command:
    - echo window
    - echo pane
    - mongo
    - echo current
    - connect
